Description: <short summary of the patch>
 .
 libseccomp (2.3.3-4.lnd.2) unstable; urgency=medium
 .
   * add loongarch64 support
 .
   -- heyanbing <heyanbing@loongson.cn> Wed Aug 26 15:35:27 CST 2020

---

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2022-03-25

--- libseccomp-2.3.3.orig/src/arch-syscall-dump.c
+++ libseccomp-2.3.3/src/arch-syscall-dump.c
@@ -43,6 +43,7 @@
 #include "arch-ppc64.h"
 #include "arch-s390.h"
 #include "arch-s390x.h"
+#include "arch-loongarch64.h"
 
 /**
  * Print the usage information to stderr and exit
@@ -133,6 +134,9 @@ int main(int argc, char *argv[])
 		case SCMP_ARCH_S390X:
 			sys = s390x_syscall_iterate(iter);
 			break;
+		case SCMP_ARCH_LOONGARCH64:
+			sys = loongarch64_syscall_iterate(iter);
+			break;
 		default:
 			/* invalid arch */
 			exit_usage(argv[0]);
--- libseccomp-2.3.3.orig/src/arch-syscall-validate
+++ libseccomp-2.3.3/src/arch-syscall-validate
@@ -414,7 +414,7 @@ function dump_sys_loongarch64() {
 #  Dump the library's syscall table to stdout.
 #
 function dump_lib_loongarch64() {
-	dump_lib_arch riscv64
+	dump_lib_arch loongarch64
 }
 
 #
@@ -637,6 +637,7 @@ if [[ $opt_arches == "" ]]; then
 	opt_arches=" \
 		x86 x86_64 x32 \
 		arm aarch64 \
+		loongarch64 \
 		mips mips64 mips64n32 \
 		ppc ppc64 \
 		s390 s390x"
--- libseccomp-2.3.3.orig/src/python/seccomp.pyx
+++ libseccomp-2.3.3/src/python/seccomp.pyx
@@ -160,6 +160,7 @@ cdef class Arch:
     X32 - 64-bit x86 using the x32 ABI
     ARM - ARM
     AARCH64 - 64-bit ARM
+    LOONGARCH64 - 64-bit LOONGARCH
     MIPS - MIPS O32 ABI
     MIPS64 - MIPS 64-bit ABI
     MIPS64N32 - MIPS N32 ABI
@@ -180,6 +181,7 @@ cdef class Arch:
     X32 = libseccomp.SCMP_ARCH_X32
     ARM = libseccomp.SCMP_ARCH_ARM
     AARCH64 = libseccomp.SCMP_ARCH_AARCH64
+    LOONGARCH64 = libseccomp.SCMP_ARCH_LOONGARCH64
     MIPS = libseccomp.SCMP_ARCH_MIPS
     MIPS64 = libseccomp.SCMP_ARCH_MIPS64
     MIPS64N32 = libseccomp.SCMP_ARCH_MIPS64N32
@@ -216,6 +218,8 @@ cdef class Arch:
                 self._token = libseccomp.SCMP_ARCH_ARM
             elif arch == libseccomp.SCMP_ARCH_AARCH64:
                 self._token = libseccomp.SCMP_ARCH_AARCH64
+            elif arch == libseccomp.SCMP_ARCH_LOONGARCH64:
+                self._token = libseccomp.SCMP_ARCH_LOONGARCH64
             elif arch == libseccomp.SCMP_ARCH_MIPS:
                 self._token = libseccomp.SCMP_ARCH_MIPS
             elif arch == libseccomp.SCMP_ARCH_MIPS64:
--- libseccomp-2.3.3.orig/src/system.c
+++ libseccomp-2.3.3/src/system.c
@@ -67,6 +67,7 @@ int sys_chk_seccomp_syscall(void)
 	case SCMP_ARCH_X86_64:
 	case SCMP_ARCH_ARM:
 	case SCMP_ARCH_AARCH64:
+	case SCMP_ARCH_LOONGARCH64:
 	case SCMP_ARCH_PPC64:
 	case SCMP_ARCH_PPC64LE:
 	case SCMP_ARCH_S390:
--- libseccomp-2.3.3.orig/tests/16-sim-arch_basic.c
+++ libseccomp-2.3.3/tests/16-sim-arch_basic.c
@@ -59,6 +59,9 @@ int main(int argc, char *argv[])
 	rc = seccomp_arch_add(ctx, SCMP_ARCH_AARCH64);
 	if (rc != 0)
 		goto out;
+	rc = seccomp_arch_add(ctx, SCMP_ARCH_LOONGARCH64);
+	if (rc != 0)
+		goto out;
 	rc = seccomp_arch_add(ctx, SCMP_ARCH_MIPSEL);
 	if (rc != 0)
 		goto out;
--- libseccomp-2.3.3.orig/tests/16-sim-arch_basic.py
+++ libseccomp-2.3.3/tests/16-sim-arch_basic.py
@@ -36,6 +36,7 @@ def test(args):
     f.add_arch(Arch("x32"))
     f.add_arch(Arch("arm"))
     f.add_arch(Arch("aarch64"))
+    f.add_arch(Arch("loongarch64"))
     f.add_arch(Arch("mipsel"))
     f.add_arch(Arch("mipsel64"))
     f.add_arch(Arch("mipsel64n32"))
--- libseccomp-2.3.3.orig/tests/23-sim-arch_all_le_basic.c
+++ libseccomp-2.3.3/tests/23-sim-arch_all_le_basic.c
@@ -59,6 +59,9 @@ int main(int argc, char *argv[])
 	rc = seccomp_arch_add(ctx, seccomp_arch_resolve_name("aarch64"));
 	if (rc != 0)
 		goto out;
+	rc = seccomp_arch_add(ctx, seccomp_arch_resolve_name("loongarch64"));
+	if (rc != 0)
+		goto out;
 	rc = seccomp_arch_add(ctx, seccomp_arch_resolve_name("mipsel"));
 	if (rc != 0)
 		goto out;
--- libseccomp-2.3.3.orig/tests/23-sim-arch_all_le_basic.py
+++ libseccomp-2.3.3/tests/23-sim-arch_all_le_basic.py
@@ -36,6 +36,7 @@ def test(args):
     f.add_arch(Arch("x32"))
     f.add_arch(Arch("arm"))
     f.add_arch(Arch("aarch64"))
+    f.add_arch(Arch("loongarch64"))
     f.add_arch(Arch("mipsel"))
     f.add_arch(Arch("mipsel64"))
     f.add_arch(Arch("mipsel64n32"))
--- libseccomp-2.3.3.orig/tests/regression
+++ libseccomp-2.3.3/tests/regression
@@ -23,7 +23,7 @@
 
 GLBL_ARCH_LE_SUPPORT=" \
 	x86 x86_64 x32 \
-	arm aarch64 \
+	arm aarch64 loongarch64\
 	mipsel mipsel64 mipsel64n32 \
 	ppc64le"
 GLBL_ARCH_BE_SUPPORT=" \
@@ -707,7 +707,7 @@ function run_test_live() {
 
 	# setup the arch specific return values
 	case "$arch" in
-	x86|x86_64|x32|arm|aarch64|parisc|parisc64|ppc|ppc64|ppc64le|ppc|s390|s390x)
+	x86|x86_64|x32|arm|aarch64|parisc|parisc64|ppc|ppc64|ppc64le|ppc|s390|s390x|loongarch64)
 		rc_kill=159
 		rc_allow=160
 		rc_trap=161
--- libseccomp-2.3.3.orig/tools/scmp_bpf_sim.c
+++ libseccomp-2.3.3/tools/scmp_bpf_sim.c
@@ -253,6 +253,8 @@ int main(int argc, char *argv[])
 				arch = AUDIT_ARCH_ARM;
 			else if (strcmp(optarg, "aarch64") == 0)
 				arch = AUDIT_ARCH_AARCH64;
+			else if (strcmp(optarg, "loongarch64") == 0)
+				arch = AUDIT_ARCH_AARCH64;
 			else if (strcmp(optarg, "mips") == 0)
 				arch = AUDIT_ARCH_MIPS;
 			else if (strcmp(optarg, "mipsel") == 0)
--- libseccomp-2.3.3.orig/tools/util.c
+++ libseccomp-2.3.3/tools/util.c
@@ -74,7 +74,7 @@
 #endif
 #elif __PPC__
 #define ARCH_NATIVE		AUDIT_ARCH_PPC
-#elif __loongarch 
+#elif __loongarch64 
 /* kernel audit support for loongarch is not yet ready */
 //#define EM_RISCV		243
 #define AUDIT_ARCH_LOONGARCH64	(EM_LOONGARCH|__AUDIT_ARCH_64BIT|__AUDIT_ARCH_LE)
--- libseccomp-2.3.3.orig/src/python/libseccomp.pxd
+++ libseccomp-2.3.3/src/python/libseccomp.pxd
@@ -37,6 +37,7 @@ cdef extern from "seccomp.h":
         SCMP_ARCH_X32
         SCMP_ARCH_ARM
         SCMP_ARCH_AARCH64
+        SCMP_ARCH_LOONGARCH64
         SCMP_ARCH_MIPS
         SCMP_ARCH_MIPS64
         SCMP_ARCH_MIPS64N32
